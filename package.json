{
  "name": "giftgenius-ai",
  "version": "1.0.0",
  "private": true,
  "description": "GiftGenius AI - Smart AI-Powered Gift Shop (React + Django REST Framework)",
  "scripts": {
    "frontend:install": "npm --prefix frontend install",
    "frontend:dev": "npm --prefix frontend run dev",
    "frontend:build": "npm --prefix frontend run build"
  }
}
